# FastCDP API


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

``` python
len(_cdp_domains), [d['domain'] for d in _cdp_domains[:5]]
```

    (55, ['Accessibility', 'Animation', 'Audits', 'Autofill', 'BackgroundService'])

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L26"
target="_blank" style="float:right; font-size:smaller">source</a>

### cdp_search

``` python

def cdp_search(
    q:str
):

```

*Search CDP domains and commands by name or description*

``` python
cdp_search('target')[:100]
```

    'Audits.checkFormsIssues: Runs the form issues check for the target page. Found issues are reported\nu'

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L49"
target="_blank" style="float:right; font-size:smaller">source</a>

### cdp_conninfo

``` python

def cdp_conninfo(
    p:NoneType=None
):

```

*Call self as a function.*

On MacOS, the connection info is stored in
`~/Library/Application Support/Google/Chrome/DevToolsActivePort`.

``` python
conninfo = cdp_conninfo()
conninfo
```

    '9222/devtools/browser/6c364cd1-dae4-4e9e-857d-80fc243dbc61'

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L55"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP

``` python

def CDP(
    wsconn:NoneType=None, debug:bool=False
):

```

*Chrome DevTools Protocol connection with event support*

``` python
# await cdp.close()
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L117"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.remote

``` python

async def remote(
    port:int=9222, debug:NoneType=None
):

```

*Connect via Chrome remote debugging HTTP endpoint*

``` python
cdp = await CDP.remote()
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L125"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.pages

``` python

async def pages(
    
):

```

*Call self as a function.*

``` python
ps = await cdp.pages
pg = first(p for p in ps if 'example.com' in p['url'])
pg['title']
```

    'Example Domain'

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L158"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDPDomain

``` python

def CDPDomain(
    cdp, domain
):

```

*Initialize self. See help(type(self)) for accurate signature.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L149"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDPMethod

``` python

def CDPMethod(
    cdp, domain, method
):

```

*Initialize self. See help(type(self)) for accurate signature.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L175"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.eval

``` python

async def eval(
    expr, sid:NoneType=None
):

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L171"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.attach

``` python

async def attach(
    tid
):

```

*Call self as a function.*

``` python
tid = pg['targetId']
sid = await cdp.attach(tid)
await cdp.eval('document.title', sid)
```

    'Example Domain'

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L188"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.wait_event

``` python

async def wait_event(
    event, timeout:int=10
):

```

*Call self as a function.*

------------------------------------------------------------------------

### on

``` python

def on(
    event
):

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L212"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.wait_for_selector

``` python

async def wait_for_selector(
    sel, sid:NoneType=None, timeout:int=10
):

```

*Wait for CSS selector to match an element*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L202"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.wait_for

``` python

async def wait_for(
    expr, sid:NoneType=None, timeout:int=10
):

```

*Wait for JS expression to be truthy, return its value*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L195"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.wait_load

``` python

async def wait_load(
    sid:NoneType=None, timeout:int=10
):

```

*Call self as a function.*

``` python
t = await cdp.target.createTarget(url='about:blank')
sid = await cdp.attach(t)
page = await cdp.page.enable(sid=sid)

await cdp.page.navigate(sid=sid, url='https://httpbin.org/forms/post')
await cdp.wait_for_selector('form', sid)
```

    True

``` python
await cdp.target.closeTarget(targetId=t)
```

    True

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L225"
target="_blank" style="float:right; font-size:smaller">source</a>

### PageDomain

``` python

def PageDomain(
    sid, domain
):

```

*Initialize self. See help(type(self)) for accurate signature.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L236"
target="_blank" style="float:right; font-size:smaller">source</a>

### Page

``` python

def Page(
    cdp, t, sid, owned:bool=False
):

```

*Initialize self. See help(type(self)) for accurate signature.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L278"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.remote_page

``` python

async def remote_page(
    port:int=9222, debug:NoneType=None
):

```

*Connect via remote debugging and return Page for the active tab*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L270"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.active_page

``` python

async def active_page(
    
):

```

*Call self as a function.*

``` python
page = await CDP.remote_page()
await page.eval('document.title')
```

    'l:00_core←nbs←fastcdp←aai-ws - SolveIT'

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L289"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.new_page

``` python

async def new_page(
    
):

```

*Create a new tab, return Page*

``` python
page = await cdp.new_page()
await page.page.navigate(url='https://httpbin.org/forms/post')
await page.wait_for_selector('form')
```

    True

``` python
page.is_open
```

    True

``` python
await page.close()
```

``` python
page.is_open
```

    True

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L319"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.goto

``` python

async def goto(
    url, sid:NoneType=None, kwargs:VAR_KEYWORD
):

```

*Navigate to url and wait for load+idle*

------------------------------------------------------------------------

### wait_ready

``` python

def wait_ready(
    sid:NoneType=None, timeout:int=10, idle_ms:int=500
):

```

*Context manager: subscribes before action, waits for load+idle after*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L297"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.wait_for_ready

``` python

async def wait_for_ready(
    sid:NoneType=None, timeout:int=10, idle_ms:int=500
):

```

*Wait until network is idle for idle_ms*

``` python
page = await Page.new(cdp=cdp)
await page.goto('https://httpbin.org/forms/post')
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L325"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.screenshot

``` python

async def screenshot(
    sid:NoneType=None
):

```

*Call self as a function.*

``` python
await asyncio.sleep(0.2)
```

``` python
img = await page.screenshot()
# img
```

``` python
await page.close()
```

``` python
await cdp.close()
```

## LLMs and accessibility

``` python
cdp = await CDP.remote()
```

``` python
page = await cdp.new_page()
await page.goto('https://httpbin.org/forms/post')
await page.eval('document.title')
```

    ''

``` python
await page.accessibility.enable()
tree = await page.accessibility.getFullAXTree()
len(tree)
```

    77

``` python
tree[0]
```

    {'nodeId': '69',
     'ignored': False,
     'role': {'type': 'internalRole', 'value': 'RootWebArea'},
     'chromeRole': {'type': 'internalRole', 'value': 144},
     'name': {'type': 'computedString',
      'value': '',
      'sources': [{'type': 'relatedElement', 'attribute': 'aria-labelledby'},
       {'type': 'attribute', 'attribute': 'aria-label'},
       {'type': 'attribute', 'attribute': 'aria-label', 'superseded': True},
       {'type': 'relatedElement', 'nativeSource': 'title'}]},
     'properties': [{'name': 'focusable',
       'value': {'type': 'booleanOrUndefined', 'value': True}},
      {'name': 'focused', 'value': {'type': 'booleanOrUndefined', 'value': True}},
      {'name': 'url',
       'value': {'type': 'string', 'value': 'https://httpbin.org/forms/post'}}],
     'childIds': ['71'],
     'backendDOMNodeId': 69,
     'frameId': '523C62BB46692DDED9606FB1D00591B0'}

``` python
await page.close()
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L352"
target="_blank" style="float:right; font-size:smaller">source</a>

### AXTree

``` python

def AXTree(
    raw
):

```

*Chrome accessibility tree node with compact repr*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L331"
target="_blank" style="float:right; font-size:smaller">source</a>

### AXNode

``` python

def AXNode(
    raw
):

```

*Chrome accessibility tree node with compact repr*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L363"
target="_blank" style="float:right; font-size:smaller">source</a>

### build_ax_tree

``` python

def build_ax_tree(
    nodes
):

```

*Build AXNode tree from flat CDP accessibility node list*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L382"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.ax_tree

``` python

async def ax_tree(
    sid:NoneType=None
):

```

*Get accessibility tree for session*

``` python
page = await cdp.new_page()
await page.goto('https://httpbin.org/forms/post')
```

``` python
root = await page.ax_tree()
root
```

<div class="prose">

- **RootWebArea** “” `focusable=True`
  `url=https://httpbin.org/forms/post` \[#68\]
  - **LabelText** “” \[#75\]
    - **StaticText** “Customer name:” \[#37\]
    - **textbox** “Customer name:” `focusable=True` `editable=plaintext`
      `settable=True` \[#36\]
  - **LabelText** “” \[#78\]
    - **StaticText** “Telephone:” \[#38\]
    - **textbox** “Telephone:” `focusable=True` `editable=plaintext`
      `settable=True` \[#57\]
  - **LabelText** “” \[#81\]
    - **StaticText** “E-mail address:” \[#39\]
    - **textbox** “E-mail address:” `focusable=True`
      `editable=plaintext` `settable=True` \[#58\]
  - **group** “Pizza Size” \[#83\]
    - **Legend** “” \[#84\]
      - **StaticText** “Pizza Size” \[#40\]
    - **radio** ” Small” `focusable=True` \[#60\]
    - **radio** ” Medium” `focusable=True` \[#61\]
    - **radio** ” Large” `focusable=True` \[#62\]
  - **group** “Pizza Toppings” \[#91\]
    - **Legend** “” \[#92\]
      - **StaticText** “Pizza Toppings” \[#44\]
    - **checkbox** ” Bacon” `focusable=True` \[#63\]
    - **checkbox** ” Extra Cheese” `focusable=True` \[#64\]
    - **checkbox** ” Onion” `focusable=True` \[#65\]
    - **checkbox** ” Mushroom” `focusable=True` \[#66\]
  - **LabelText** “” \[#102\]
    - **StaticText** “Preferred delivery time:” \[#54\]
    - **InputTime** “Preferred delivery time:” `focusable=True`
      `settable=True` \[#67\]
      - **spinbutton** “Hours Hours” `focusable=True` `settable=True`
        `valuemin=1` `valuemax=12` \[#106\]
        - **StaticText** “–” \[#49\]
      - **StaticText** “:” \[#50\]
      - **spinbutton** “Minutes Minutes” `focusable=True`
        `settable=True` `valuemax=59` \[#108\]
        - **StaticText** “–” \[#51\]
      - **StaticText** ” ” \[#52\]
      - **spinbutton** “AM/PM AM/PM” `focusable=True` `settable=True`
        `valuemin=1` `valuemax=2` \[#110\]
        - **StaticText** “–” \[#53\]
      - **button** “Show time picker” `focusable=True` `hasPopup=menu`
        \[#69\]
  - **LabelText** “” \[#112\]
    - **StaticText** “Delivery instructions:” \[#55\]
    - **textbox** “Delivery instructions:” `focusable=True`
      `editable=plaintext` `settable=True` `multiline=True` \[#59\]
  - **button** “Submit order” `focusable=True` \[#115\]
    - **StaticText** “Submit order” \[#56\]

</div>

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L402"
target="_blank" style="float:right; font-size:smaller">source</a>

### AXNode.find_all

``` python

def find_all(
    role:NoneType=None, name:NoneType=None
):

```

*Find all descendants matching role and/or name substring*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L396"
target="_blank" style="float:right; font-size:smaller">source</a>

### AXNode.find_id

``` python

def find_id(
    role:NoneType=None, name:NoneType=None
):

```

*Find first descendant matching role and/or name substring*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L389"
target="_blank" style="float:right; font-size:smaller">source</a>

### AXNode.find

``` python

def find(
    role:NoneType=None, name:NoneType=None
):

```

*Find first descendant matching role and/or name substring*

``` python
nmid = root.find_id('textbox', 'Customer name')
phid = root.find_id('textbox', 'Telephone')
nmid
```

    36

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L421"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.click

``` python

async def click(
    backendNodeId, sid:NoneType=None
):

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L417"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.js_node_run

``` python

async def js_node_run(
    code, backendNodeId, sid:NoneType=None
):

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L411"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.js_node

``` python

async def js_node(
    fn, backendNodeId, sid:NoneType=None
):

```

*Call self as a function.*

``` python
await page.DOM.focus(backendNodeId=nmid)
await page.input.insertText(text='Jeremy Howard')

await page.DOM.focus(backendNodeId=phid)
await page.input.insertText(text='555-1234')
```

    {}

``` python
await page.click(root.find_id('radio', 'Large'))
await page.click(root.find_id('checkbox', 'Extra Cheese'))
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L426"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.fill_text

``` python

async def fill_text(
    backendNodeId, text, sid:NoneType=None
):

```

*Call self as a function.*

``` python
await page.fill_text(root.find_id('textbox', 'Delivery'), 'Ring the doorbell twice')
```

``` python
await page.js_node_run('this.value = "18:30"', root.find_id('InputTime', 'delivery time'))
```

    {'type': 'undefined'}

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L432"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.click_and_wait

``` python

async def click_and_wait(
    backendNodeId, sid:NoneType=None, kwargs:VAR_KEYWORD
):

```

*Click element and wait for load+idle*

``` python
await page.click_and_wait(root.find_id('button', 'Submit order'))
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L437"
target="_blank" style="float:right; font-size:smaller">source</a>

### cdp_yolo

``` python

def cdp_yolo(
    
):

```

*Allow all CDP classes in safepyrun*
