# FastCDP API


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

``` python
len(_cdp_domains), [d['domain'] for d in _cdp_domains[:5]]
```

    (55, ['Accessibility', 'Animation', 'Audits', 'Autofill', 'BackgroundService'])

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L26"
target="_blank" style="float:right; font-size:smaller">source</a>

### cdp_search

``` python

def cdp_search(
    q:str
):

```

*Search CDP domains and commands by name or description*

``` python
cdp_search('target')[:100]
```

    'Audits.checkFormsIssues: Runs the form issues check for the target page. Found issues are reported\nu'

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L49"
target="_blank" style="float:right; font-size:smaller">source</a>

### cdp_conninfo

``` python

def cdp_conninfo(
    p:NoneType=None
):

```

On MacOS, the connection info is stored in
`~/Library/Application Support/Google/Chrome/DevToolsActivePort`.

``` python
conninfo = cdp_conninfo()
conninfo
```

    '9222/devtools/browser/3168e5a6-460e-4647-a8f5-9efe1b203e36'

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L55"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP

``` python

def CDP(
    wsconn:NoneType=None, debug:bool=False
):

```

*Chrome DevTools Protocol connection with event support*

``` python
# await cdp.close()
```

``` python
cdp = await CDP.connect()
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L116"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.pages

``` python

async def pages(
    
):

```

``` python
ps = await cdp.pages
pg = first(p for p in ps if 'example.com' in p['url'])
pg['title']
```

    '3. Example Domain'

``` python
await cdp.close()
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L149"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDPDomain

``` python

def CDPDomain(
    cdp, domain
):

```

*Initialize self. See help(type(self)) for accurate signature.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L140"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDPMethod

``` python

def CDPMethod(
    cdp, domain, method
):

```

*Initialize self. See help(type(self)) for accurate signature.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L166"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.eval

``` python

async def eval(
    expr, sid:NoneType=None
):

```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L162"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.attach

``` python

async def attach(
    tid
):

```

``` python
tid = pg['targetId']
sid = await cdp.attach(tid)
await cdp.eval('document.title', sid)
```

    '3. Example Domain'

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L179"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.wait_event

``` python

async def wait_event(
    event, timeout:int=10
):

```

------------------------------------------------------------------------

### on

``` python

def on(
    event
):

```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L203"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.wait_for_selector

``` python

async def wait_for_selector(
    sel, sid:NoneType=None, timeout:int=10
):

```

*Wait for CSS selector to match an element*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L193"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.wait_for

``` python

async def wait_for(
    expr, sid:NoneType=None, timeout:int=10
):

```

*Wait for JS expression to be truthy, return its value*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L186"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.wait_load

``` python

async def wait_load(
    sid:NoneType=None, timeout:int=10
):

```

``` python
t = await cdp.target.createTarget(url='about:blank')
sid = await cdp.attach(t)
page = await cdp.page.enable(sid=sid)

await cdp.page.navigate(sid=sid, url='https://httpbin.org/forms/post')
await cdp.wait_for_selector('form', sid)
```

    True

``` python
await cdp.target.closeTarget(targetId=t)
```

    True

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L216"
target="_blank" style="float:right; font-size:smaller">source</a>

### PageDomain

``` python

def PageDomain(
    sid, domain
):

```

*Initialize self. See help(type(self)) for accurate signature.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L227"
target="_blank" style="float:right; font-size:smaller">source</a>

### Page

``` python

def Page(
    cdp, t, sid, owned:bool=False
):

```

*Initialize self. See help(type(self)) for accurate signature.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L261"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.new_page

``` python

async def new_page(
    
):

```

*Create a new tab, return Page*

``` python
page = await cdp.new_page()
await page.page.navigate(url='https://httpbin.org/forms/post')
await page.wait_for_selector('form')
```

    True

``` python
await page.close()
await cdp.close()
```

``` python
page = await Page.new()
```

``` python
await page.page.navigate(url='https://httpbin.org/forms/post')
await page.wait_for_selector('form')
```

    True

``` python
page.is_open
```

    True

``` python
await page.close()
```

``` python
page.is_open
```

    False

------------------------------------------------------------------------

### wait_ready

``` python

def wait_ready(
    sid:NoneType=None, timeout:int=10, idle_ms:int=500
):

```

*Context manager: subscribes before action, waits for load+idle after*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L268"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.wait_for_ready

``` python

async def wait_for_ready(
    sid:NoneType=None, timeout:int=10, idle_ms:int=500
):

```

*Wait until network is idle for idle_ms*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L291"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.goto

``` python

async def goto(
    url, sid:NoneType=None, kwargs:VAR_KEYWORD
):

```

*Navigate to url and wait for load+idle*

``` python
page = await Page.new()
await page.goto('https://httpbin.org/forms/post')
await page.eval('document.title')
```

    '6. httpbin.org/forms/post'

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L297"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.screenshot

``` python

async def screenshot(
    sid:NoneType=None
):

```

``` python
img = await page.screenshot()
# img
```

``` python
await page.close()
```

## LLMs and accessibility

``` python
cdp = await CDP.connect()
```

``` python
page = await cdp.new_page()
await page.goto('https://httpbin.org/forms/post')
await page.eval('document.title')
```

    '5. httpbin.org/forms/post'

``` python
await page.accessibility.enable()
tree = await page.accessibility.getFullAXTree()
len(tree)
```

    90

``` python
tree[0]
```

    {'nodeId': '2',
     'ignored': False,
     'role': {'type': 'internalRole', 'value': 'RootWebArea'},
     'chromeRole': {'type': 'internalRole', 'value': 144},
     'name': {'type': 'computedString',
      'value': '5. httpbin.org/forms/post',
      'sources': [{'type': 'relatedElement', 'attribute': 'aria-labelledby'},
       {'type': 'attribute', 'attribute': 'aria-label'},
       {'type': 'attribute', 'attribute': 'aria-label', 'superseded': True},
       {'type': 'relatedElement',
        'value': {'type': 'computedString', 'value': '5. httpbin.org/forms/post'},
        'nativeSource': 'title'}]},
     'properties': [{'name': 'focusable',
       'value': {'type': 'booleanOrUndefined', 'value': True}},
      {'name': 'url',
       'value': {'type': 'string', 'value': 'https://httpbin.org/forms/post'}}],
     'childIds': ['19'],
     'backendDOMNodeId': 2,
     'frameId': 'CC0919B92BC22D0E81B85A567CD14BED'}

``` python
await page.close()
```

    True

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L324"
target="_blank" style="float:right; font-size:smaller">source</a>

### AXTree

``` python

def AXTree(
    raw
):

```

*Chrome accessibility tree node with compact repr*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L303"
target="_blank" style="float:right; font-size:smaller">source</a>

### AXNode

``` python

def AXNode(
    raw
):

```

*Chrome accessibility tree node with compact repr*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L335"
target="_blank" style="float:right; font-size:smaller">source</a>

### build_ax_tree

``` python

def build_ax_tree(
    nodes
):

```

*Build AXNode tree from flat CDP accessibility node list*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L354"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.ax_tree

``` python

async def ax_tree(
    sid:NoneType=None
):

```

*Get accessibility tree for session*

``` python
page = await cdp.new_page()
await page.goto('https://httpbin.org/forms/post')
```

``` python
root = await page.ax_tree()
root
```

<div class="prose">

- **RootWebArea** “5. httpbin.org/forms/post” `focusable=True`
  `url=https://httpbin.org/forms/post` \[#15\]
  - **LabelText** “” \[#24\]
    - **StaticText** “Customer name:” \[#64\]
      - **InlineTextBox** “Customer name:”
    - **textbox** “Customer name:” `focusable=True` `editable=plaintext`
      `settable=True` \[#3\]
  - **LabelText** “” \[#27\]
    - **StaticText** “Telephone:” \[#65\]
      - **InlineTextBox** “Telephone:”
    - **textbox** “Telephone:” `focusable=True` `editable=plaintext`
      `settable=True` \[#4\]
  - **LabelText** “” \[#30\]
    - **StaticText** “E-mail address:” \[#66\]
      - **InlineTextBox** “E-mail address:”
    - **textbox** “E-mail address:” `focusable=True`
      `editable=plaintext` `settable=True` \[#5\]
  - **group** “Pizza Size” \[#32\]
    - **Legend** “” \[#33\]
      - **StaticText** “Pizza Size” \[#67\]
        - **InlineTextBox** “Pizza Size”
    - **radio** ” Small” `focusable=True` \[#7\]
    - **radio** ” Medium” `focusable=True` \[#8\]
    - **radio** ” Large” `focusable=True` \[#9\]
  - **group** “Pizza Toppings” \[#40\]
    - **Legend** “” \[#41\]
      - **StaticText** “Pizza Toppings” \[#71\]
        - **InlineTextBox** “Pizza Toppings”
    - **checkbox** ” Bacon” `focusable=True` \[#10\]
    - **checkbox** ” Extra Cheese” `focusable=True` \[#11\]
    - **checkbox** ” Onion” `focusable=True` \[#12\]
    - **checkbox** ” Mushroom” `focusable=True` \[#13\]
  - **LabelText** “” \[#50\]
    - **StaticText** “Preferred delivery time:” \[#76\]
      - **InlineTextBox** “Preferred delivery time:”
    - **InputTime** “Preferred delivery time:” `focusable=True`
      `settable=True` \[#14\]
      - **spinbutton** “Hours Hours” `focusable=True` `settable=True`
        `valuemin=1` `valuemax=12` \[#54\]
        - **StaticText** “–” \[#77\]
          - **InlineTextBox** “–”
      - **StaticText** “:” \[#78\]
        - **InlineTextBox** “:”
      - **spinbutton** “Minutes Minutes” `focusable=True`
        `settable=True` `valuemax=59` \[#56\]
        - **StaticText** “–” \[#79\]
          - **InlineTextBox** “–”
      - **StaticText** ” ” \[#80\]
        - **InlineTextBox** ” ”
      - **spinbutton** “AM/PM AM/PM” `focusable=True` `settable=True`
        `valuemin=1` `valuemax=2` \[#58\]
        - **StaticText** “–” \[#81\]
          - **InlineTextBox** “–”
      - **button** “Show time picker” `focusable=True` `hasPopup=menu`
        \[#16\]
  - **LabelText** “” \[#60\]
    - **StaticText** “Delivery instructions:” \[#82\]
      - **InlineTextBox** “Delivery instructions:”
    - **textbox** “Delivery instructions:” `focusable=True`
      `editable=plaintext` `settable=True` `multiline=True` \[#6\]
  - **button** “Submit order” `focusable=True` \[#63\]
    - **StaticText** “Submit order” \[#83\]
      - **InlineTextBox** “Submit order”

</div>

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L374"
target="_blank" style="float:right; font-size:smaller">source</a>

### AXNode.find_all

``` python

def find_all(
    role:NoneType=None, name:NoneType=None
):

```

*Find all descendants matching role and/or name substring*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L368"
target="_blank" style="float:right; font-size:smaller">source</a>

### AXNode.find_id

``` python

def find_id(
    role:NoneType=None, name:NoneType=None
):

```

*Find first descendant matching role and/or name substring*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L361"
target="_blank" style="float:right; font-size:smaller">source</a>

### AXNode.find

``` python

def find(
    role:NoneType=None, name:NoneType=None
):

```

*Find first descendant matching role and/or name substring*

``` python
nmid = root.find_id('textbox', 'Customer name')
phid = root.find_id('textbox', 'Telephone')
nmid
```

    3

``` python
cdp_yolo()
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L393"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.click

``` python

async def click(
    backendNodeId, sid:NoneType=None
):

```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L389"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.js_node_run

``` python

async def js_node_run(
    code, backendNodeId, sid:NoneType=None
):

```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L383"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.js_node

``` python

async def js_node(
    fn, backendNodeId, sid:NoneType=None
):

```

``` python
await page.DOM.focus(backendNodeId=nmid)
await page.input.insertText(text='Jeremy Howard')

await page.DOM.focus(backendNodeId=phid)
await page.input.insertText(text='555-1234')
```

    {}

``` python
await page.click(root.find_id('radio', 'Large'))
await page.click(root.find_id('checkbox', 'Extra Cheese'))
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L398"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.fill_text

``` python

async def fill_text(
    backendNodeId, text, sid:NoneType=None
):

```

``` python
await page.fill_text(root.find_id('textbox', 'Delivery'), 'Ring the doorbell twice')
```

``` python
await page.js_node_run('this.value = "18:30"', root.find_id('InputTime', 'delivery time'))
```

    {'type': 'undefined'}

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L404"
target="_blank" style="float:right; font-size:smaller">source</a>

### CDP.click_and_wait

``` python

async def click_and_wait(
    backendNodeId, sid:NoneType=None, kwargs:VAR_KEYWORD
):

```

*Click element and wait for load+idle*

``` python
await page.click_and_wait(root.find_id('button', 'Submit order'))
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcdp/blob/main/fastcdp/core.py#L409"
target="_blank" style="float:right; font-size:smaller">source</a>

### cdp_yolo

``` python

def cdp_yolo(
    
):

```

*Allow all CDP classes in safepyrun*
